/**
 * Barrel exports for the providers module.
 *
 * Re-exports the composed `Providers` component and its props so they
 * can be imported from a single entry point.
 *
 * @example
 * import { Providers, type ProvidersProps } from "@/providers";
 */

export { Providers } from "./Providers";
export type { ProvidersProps } from "./Providers";